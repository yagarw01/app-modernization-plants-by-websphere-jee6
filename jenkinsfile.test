pipeline {
  agent any

  stages {

    stage('Build application binary') {
      steps {
        git ("https://github.com/odrodrig/app-modernization-plants-by-websphere-jee6.git")
        container('maven') {
            sh """
            #!/bin/bash
            mvn -B -DskipTests clean package
            """            
        }
      }
    }

  }
}
